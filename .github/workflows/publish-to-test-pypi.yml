name: Publish Python 🐍 distributions 📦 to Test PyPI

on:
  push:
    branches:    
      - 'test/**'

jobs:
  testpypi:

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: [3.9, '3.10', '3.11']
        include:
          - os: ubuntu-latest
            cxx: g++
            cc: gcc
            label: linux-64
          - os: macos-latest
            cxx: clang++
            cc: clang
            label: osx-64
    

    name: ${{ matrix.label }}-Python-${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}


    defaults:
      run:
        shell: bash -l {0}


    steps:
    - uses: actions/checkout@v2


    - name: Set Up Homebrew
      if: runner.os == 'macOS'
      uses: Homebrew/actions/setup-homebrew@master


    - name: Install Homebrew Dependencies
      if: runner.os == 'macOS'
      run: |
        brew install libomp


    - name: Cache Conda
      id: cached-conda-dependencies
      uses: actions/cache@v2
      with:
        path: |
          ~/.cache/pypoetry
          ~/miniconda3/envs/noiseflow
        key: ${{ runner.os }}-python${{ matrix.python-version }}-${{ hashFiles('**/environment.yml') }}-${{ hashFiles('**/poetry.lock') }}
      

    - name: Set Up Conda Environment with Miniconda
      uses: conda-incubator/setup-miniconda@v2
      with:
        miniconda-version: "latest"
        python-version: ${{ matrix.python-version }}
        channels: conda-forge, defaults
        auto-update-conda: true
        auto-activate-base: false
        use-only-tar-bz2: true
        activate-environment: noiseflow


    - name: Set Up Poetry in Conda Environment
      uses: snok/install-poetry@v1
      with:
        version: 1.2.2
        virtualenvs-create: false
        installer-parallel: true


    - name: Update Conda and Poetry Environment
      if: steps.cached-conda-dependencies.outputs.cache-hit != 'true'
      run: |
        conda env update -n noiseflow -f environment.yml
        poetry install --no-interaction
     

    - name: Install Source-Code from Github
      run: |
        CC=${{ matrix.cc }} CXX=${{ matrix.cxx }} python build_extern.py -c github-actions


    - name: Build and Publish
      run: |
        if [[ "${{ matrix.os }}" == "macos-latest" && "${{ matrix.python-version }}" == "3.10" ]]; then
          CXX=${{ matrix.cxx }} NOISEFLOW_USE_CPP=1 poetry build
        else
          CXX=${{ matrix.cxx }} NOISEFLOW_USE_CPP=1 poetry build -f wheel
        fi
        poetry env info
        poetry config repositories.test-pypi https://test.pypi.org/legacy/
        poetry publish --repository test-pypi --username __token__ --password ${{ secrets.TEST_PYPI_TOKEN }}

